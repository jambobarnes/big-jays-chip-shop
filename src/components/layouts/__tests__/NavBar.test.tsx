import React from 'react';
import { render, screen } from '@testing-library/react';
import NavBar from '../NavBar';
import Footer from '../Footer';

test.todo('The navbar includes the logo (small and large screens) and basket icon')